# Sprints

MODE: MANUAL
BRAAT BLOCK: Sprint map
MISSION: Continue locally without losing the execution thread.

Each sprint uses the same rule:

```text
one active execution thread -> proof output -> review -> next sprint
```

Current repo includes all sprint run cards. The proof path is executable now through Sprint 07.
