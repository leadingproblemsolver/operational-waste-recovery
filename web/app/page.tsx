export default function HomePage() {
  return (
    <main className="failure">
      <div className="eyebrow">ReworkTrace</div>
      <h1>Evidence-first review surface</h1>
      <p>
        Open a persisted duplicate-work finding at <code>/review/&lt;audit_id&gt;</code>. The page reads the finding from the Operational Waste Recovery HTTP API; it does not embed demo data.
      </p>
    </main>
  )
}
