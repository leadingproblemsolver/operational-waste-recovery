[build-system]
requires = []
build-backend = "build_backend"
backend-path = ["."]

[project]
name = "operational-waste-recovery"
version = "1.0.0"
description = "Local-first telemetry, duplicate-work detection, context recovery, and export tooling"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Leading Problem Solver"}]
dependencies = []

[project.scripts]
owrp = "owrp.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
